{
  "mcpServers": {
    "doc-index": {
      "command": "uv",
      "args": ["--directory", "..", "run", "doc-index-mcp"],
      "env": {
        "MCP_WORKING_DIR": "${workspaceFolder}"
      }
    }
  }
}
